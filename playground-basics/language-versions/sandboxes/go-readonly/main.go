package main

import "fmt"

func main() {
	// Этот файл нельзя редактировать — он read-only.
	// Полезно для демонстрации: студент видит код, запускает, но не меняет.
	fmt.Println("Hello from a read-only sandbox!")
	fmt.Println("Попробуйте изменить текст — не получится.")
}

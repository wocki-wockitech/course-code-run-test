package main

// Этот файл readonly — студент видит его, но не может изменить.
// Используется для показа тестов или эталонного кода.

import "testing"

func TestMain(t *testing.T) {
	t.Log("Этот тест виден, но файл нельзя редактировать")
}
